import Link from 'next/link';import {requireAccess} from '~/server/access';import {db} from '~/server/db';import {Shell,Heading} from '~/app/ui';import PatientDirectory from '~/app/patient-directory';
export const dynamic='force-dynamic';
export default async function PatientsPage(){await requireAccess();const patients=await db.patient.findMany({where:{archivedAt:null},include:{visits:{orderBy:{date:'desc'},take:1},_count:{select:{encounters:true}}},orderBy:{updatedAt:'desc'}});return <Shell><main className="page"><Heading eyebrow="YOUR WORKSPACE" title="病人档案 / Patients" description="搜索并打开病人记录。" action={<Link className="primary-button" href="/patients/new">新增病人 / New patient</Link>}/><PatientDirectory patients={patients.map(p=>({id:p.id,identifier:p.identifier,payer:p.payer,area:p.primaryTreatmentArea,count:p._count.encounters,date:p.visits[0]?.date??''}))}/></main></Shell>}
