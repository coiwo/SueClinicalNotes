'use client';import {useState} from 'react';import Link from 'next/link';
type Row={id:string;identifier:string;payer:string;area:string;count:number;date:string};
export default function PatientDirectory({patients}:{patients:Row[]}){const[q,setQ]=useState('');const visible=patients.filter(p=>`${p.identifier} ${p.payer} ${p.area}`.toLowerCase().includes(q.toLowerCase()));return <><label className="search directory-search"><span>搜索病人 / Search</span><input value={q} onChange={e=>setQ(e.target.value)} placeholder="编号、部位或保险公司"/></label><div className="patient-directory">{visible.map(p=><Link className="directory-card" key={p.id} href={`/patients/${p.id}`}><div><span className="tag">{p.payer.replace('_',' / ')}</span><span className="tag">{p.area}</span></div><h2>{p.identifier}</h2><p>{p.count} 次实际诊疗 · 最近 {p.date||'尚无记录'}</p><span className="chevron">›</span></Link>)}{!visible.length&&<p>没有找到匹配的病人。</p>}</div></>}
